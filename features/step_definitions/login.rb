Given(/^a user on the login page$/) do
  pending
end

When(/^the user enters valid credentials and successfully logs in$/) do
  pending
end

Then(/^the user is redirected to their user page$/) do
  pending
end